# Minimal requirements for Vercel deployment
# Core FastAPI (essential only)
fastapi
uvicorn
pydantic

# Database (lightweight)
sqlalchemy
psycopg2-binary

# AI (essential only)
mistralai
groq

# Document processing (lightweight)
pypdf

# Utilities (minimal)
python-dotenv
httpx
aiofiles

# Security (essential)
python-jose[cryptography]
passlib[bcrypt]

# Storage
boto3

# Agent framework (if available)
agno
