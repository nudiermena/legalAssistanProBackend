{
  "version": 2,
  "builds": [
    {
      "src": "api/index-hybrid.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "250mb",
        "runtime": "python3.11",
        "installCommand": "pip install -r requirements-hybrid.txt --no-cache-dir",
        "maxDuration": 30
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index-hybrid.py"
    }
  ],
  "env": {
    "PYTHONPATH": "."
  }
}
