{
  "version": 2,
  "builds": [
    {
      "src": "api/index-minimal.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "250mb",
        "runtime": "python3.11",
        "installCommand": "pip install -r requirements-minimal.txt --no-cache-dir",
        "maxDuration": 30
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index-minimal.py"
    }
  ],
  "env": {
    "PYTHONPATH": "."
  }
}
